div(ng-controller="TypeController as type")
  section
    .container
      .row
        .col-lg-12
          br
          h1.header Select // type.type.name // 
            a.pull-right.btn.btn-primary(href='// "/types/" + type.type.id + "/create" //')
              i.fa.fa-plus
              | Add new
          pre // type.type | json //